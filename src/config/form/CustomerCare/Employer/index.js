import * as Profile from "./Profile";

export const EmployerPage = {
    Profile: {
        "default": Profile.arrayDefault,
        "vl24h": Profile.arrayVl24h,
        "vtn": Profile.arrayVTN,
        "tvn": Profile.arrayTVN,
        "mw": Profile.arrayMW,
    },
};